# MySQL Database Connection String
# Replace with your production database credentials
DATABASE_URL="mysql://username:password@localhost:3306/asset_db"

# The domain of the frontend application (for CORS)
# Replace with your production frontend URL, or use * to allow all (not recommended)
FRONTEND_URL="http://localhost:5173"

# Port for the server to listen on
PORT=3001
